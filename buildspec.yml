version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 10

    commands:
        - git clone https://github.com/dflydev/git-subsplit.git
        - cd git-subsplit
        - ./install.sh

  build:
    commands:
        # gerar fontes
        - echo Fazer SPLIT do projeto
        - cd $CODEBUILD_SRC_DIR

        - echo Finalizou OK